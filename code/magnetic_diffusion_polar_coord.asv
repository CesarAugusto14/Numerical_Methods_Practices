clc; clear variables; close all;

sigma = 5.7e7; mu = 4*pi*1e-7;

D = 1/sigma/mu; 
I = 1e3;
y0 = 1e-3;
BC = [0 mu*I/y0];

tf = 10e-6; dx =  y0/50;
dt = dx^2/2/D; 

% Time and space arrays:
t = 0:dt:tf; x = 0:dx:y0;
N = length(t); M = length(x);
% Creating matrix u:
B = zeros(N,M);
%J = zeros(N,M);